-
  const galleryData = {
    happyBee: {
      color1: 'orange',
      color2: 'fuchsia',
      getColors() { return `${this.color1}, ${this.color2}` }
    },
    oceanProject: {
      color1: 'lime',
      color2: 'cyan',
      getColors() { return `${this.color1}, ${this.color2}` }
    },
    xinpretia: {
      color1: 'white',
      color2: 'red',
      getColors() { return `${this.color1}, ${this.color2}` }
    }
  }